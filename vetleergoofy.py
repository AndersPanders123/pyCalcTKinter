import tkinter as tk, random, json; exec("with open('windowTitles.json', 'r') as file: data=json.load(file); jsonData=data['windowTitles']; random_title=random.choice(jsonData)"); window=tk.Tk(); window.title(random_title); window.geometry('500x450'); input1=tk.StringVar(); input2=tk.StringVar(); result_label=tk.Label(window, text='Result', font=('Arial', 20)); result_label.pack(); def formatResult(value): return int(value) if value.is_integer() else f'{value:.1f}'; def calculate(type): print('Hello World'); try: num1=float(input1.get()); num2=float(input2.get()); result={'add':num1+num2, 'subtract':num1-num2, 'multiply':num1*num2, 'divide':num1/num2 if num2!=0 else result_label.config(text='Cannot divide by zero!') or None, 'power':num1**num2, 'average':(num1+num2)/2, 'max':max(num1,num2), 'min':min(num1,num2), 'modulus':num1%num2, 'abs_diff':abs(num1-num2)}.get(type, result_label.config(text='Please enter valid numbers :)') or None); result_label.config(text=f'Result: {formatResult(result)}' if result is not None else ''); except ValueError: result_label.config(text='Please enter valid numbers :)'); def clearInput(): input1_entry.delete(0, tk.END); input2_entry.delete(0, tk.END); result_label.config(text='Result'); tk.Label(window, text='Enter First Number', font=('Arial', 16)).pack(pady=5); input1_entry=tk.Entry(window, textvariable=input1, font=('Arial', 16)); input1_entry.pack(pady=5); tk.Label(window, text='Enter Second Number', font=('Arial', 16)).pack(pady=5); input2_entry=tk.Entry(window, textvariable=input2, font=('Arial', 16)); input2_entry.pack(pady=5); button_clear_frame=tk.Frame(window); button_clear_frame.pack(pady=0); button_frame=tk.Frame(window); button_frame.pack(pady=0); button_frame2=tk.Frame(window); button_frame2.pack(pady=0); tk.Button(button_clear_frame, text=' Clear Numbers ', font=('Arial', 16), command=clearInput).pack(side=tk.LEFT, padx=0, pady=0); for (text, cmd) in [('+', 'add'), ('-', 'subtract'), ('*', 'multiply'), ('/', 'divide'), ('^', 'power'), ('%', 'modulus')]: tk.Button(button_frame, text=text, font=('Arial', 16), command=lambda t=cmd: calculate(t)).pack(side=tk.LEFT, padx=0, pady=0); for (text, cmd) in [('max', 'max'), ('min', 'min'), ('avg', 'average'), ('diff', 'abs_diff')]: tk.Button(button_frame2, text=text, font=('Arial', 16), command=lambda t=cmd: calculate(t)).pack(side=tk.LEFT, padx=0, pady=0); window.mainloop()